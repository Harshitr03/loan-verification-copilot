# Copy to .env and adjust. All optional — sensible defaults ship in docker-compose.
LVC_JWT_SECRET=change-me-in-prod
# Leave blank to run the AI assistant on the deterministic MockProvider (zero-key demo).
LVC_ANTHROPIC_API_KEY=
